import React from 'react';
import { CustomerNavbar } from '../components/CustomerNavbar';

function Product() {
  return (
    <main>
        <div>
          <CustomerNavbar />
        </div>
    </main>
  );
}

export default Product;
